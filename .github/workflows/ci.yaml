name: ML CI Workflow

permissions:
  contents: write

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  train:
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash -l {0}

    env:
      MLFLOW_TRACKING_URI: https://dagshub.com/pemujacoding/medical_cost_model.mlflow
      MLFLOW_TRACKING_USERNAME: ${{ secrets.DAGSHUB_TOKEN }}
      MLFLOW_TRACKING_PASSWORD: ${{ secrets.DAGSHUB_TOKEN }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up conda
      uses: conda-incubator/setup-miniconda@v2
      with:
        environment-file: MLProject/conda.yaml
        activate-environment: mlproject-env
        auto-activate-base: false

    # =========================
    # TRAIN MODEL (LOG KE DAGSHUB)
    # =========================
    - name: Run ML Training
      run: |
        python MLProject/modelling.py

    # =========================
    # BUILD DOCKER DARI MODEL DAGSHUB
    # =========================
    - name: Build Docker Image
      run: |
        mlflow models build-docker \
          -m models:/Online%20Training%20Medical%20Cost/Production \
          -n ${{ secrets.DOCKER_USERNAME }}/medical_cost_image

    - name: Log in to Docker Hub
      run: |
        echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

    - name: Push Docker Image
      run: |
        docker push ${{ secrets.DOCKER_USERNAME }}/medical_cost_image:latest
